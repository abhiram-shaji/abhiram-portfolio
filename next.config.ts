/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.ibb.co',
      'cvwriterssociety.ca',
      'images.moneycontrol.com'
    ],
  },
};

module.exports = nextConfig;
